using ServiceStack;
using test_autoquery.ServiceModel;

namespace test_autoquery.ServiceInterface
{
    public class MyServices : Service
    {
        public object Any(Hello request)
        {
            return new HelloResponse { Result = $"Hello, {request.Name}!" };
        }
    }
}